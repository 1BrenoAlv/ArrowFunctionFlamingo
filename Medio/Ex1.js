// Filtrar Positivos: Dado o array [ -5, 10, -2, 15, -1, 3], use filter e uma arrow function para retornar apenas os números positivos.

const numeros = [ -5, 10, -2, 15, -1, 3]

const positivos = numeros.filter(numero => numero > 0);

console.log(positivos)